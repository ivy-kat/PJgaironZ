プロジェクト概要

このプロジェクトは、Java + Maven を使った開発の学習用サンプルです。

ソースは src/main/java 配下にあります。

Maven でビルド・実行可能です。

必要環境

Java 17 以上（JDK）

Maven Wrapper（mvnw / mvnw.cmd）付きなので Maven 本体は不要

VSCode 推奨（必須ではありません）


ビルドと実行
1. コンパイル

Windows:

mvnw.cmd compile

2. 実行

Windows:

mvnw.cmd exec:java

